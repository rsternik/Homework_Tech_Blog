{{!-- Partial for each comment --}}
<div class="card">
    <h4>{{comment_text}}</h4>
    <div class="container">
        <p class="post-text">Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
</div>